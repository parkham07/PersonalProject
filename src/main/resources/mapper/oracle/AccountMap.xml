<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">

<mapper namespace="kr.co.parkham.dao.mybatis.oracle.service">
  <select id="selectName" resultType="string">
    SELECT
    TID
    FROM TB_CARD_TRANS
    WHERE ROWNUM &lt;= 1
  </select>
</mapper>